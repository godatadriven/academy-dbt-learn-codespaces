models:
  - name: stg_stripe_payment
    columns:
      - name: payment_id
        data_tests: 
          - accepted_values:
              config:
                severity: warn
                warn_if: ">0"
                error_if: ">10"
              arguments: 
                values: ['fail','succes']    
          