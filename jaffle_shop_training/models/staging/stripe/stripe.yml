sources:
 - name: stripe # this is the name we decide
   database: raw
   schema: stripe # optional, recommend to keep it in
   tables:
     - name: payments
       identifier: payment
       columns:
         - name: id
           data_tests:
             - unique
             - not_null
             
