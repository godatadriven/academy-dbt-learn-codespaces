sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        identifier: payment
        columns:
          - name: id
            data_tests:
              - unique
              - not_null      
        config: 
          freshness:
            warn_after:
              count: 10
              period: hour
            error_after:
              count: 365
              period: day
          loaded_at_field: _batched_at