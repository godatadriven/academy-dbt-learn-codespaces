sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment # what we decide
        identifier: payment # the exact table name in the warehouse
        columns:
          - name: id
            data_tests:
              - unique
              - not_null