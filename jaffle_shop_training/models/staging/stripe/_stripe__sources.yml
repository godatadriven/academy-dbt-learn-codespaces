sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        identifier: payment
        columns:
          - name: id
            data_tests:
              - unique
              - not_null
          - name: created
            tests:
              - assert_order_date_in_past
              