sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        identifier: payment
        columns:
          - name: id
            data_tests:
              - unique
              - not_null
          - name: paymentmethod
            tests:
              - dbt_utils.at_least_one