models:
  - name: fct_orders
    columns:
      - name: order_id
        data_tests:
          - unique
          - not_null
  - name: dim_customers
    columns:
      - name: customer_id
        data_tests:
          - unique
          - not_null
          